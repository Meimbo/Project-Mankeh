import 'package:flutter/material.dart';

class AgentListScreen extends StatelessWidget {
  const AgentListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: AgentListScreen(),);
  }
}

class AgentListBody extends StatelessWidget {
  const AgentListBody({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}

//sort