import 'package:flutter/material.dart';

class AgentPaymentHome extends StatelessWidget {
  const AgentPaymentHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}