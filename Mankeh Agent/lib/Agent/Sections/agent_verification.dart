import 'package:flutter/material.dart';

class VerificationHome extends StatelessWidget {
  const VerificationHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}